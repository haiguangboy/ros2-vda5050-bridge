#!/bin/bash
# build.sh - 便捷的构建脚本
# 调用重新组织后的构建脚本

echo "🔧 启动中力桥接器构建"
echo "======================"

# 检查是否在正确的目录
if [ ! -f "CMakeLists.txt" ]; then
    echo "❌ 请在cpp_version目录中运行此脚本"
    exit 1
fi

# 调用重新组织后的构建脚本
exec scripts/build/build_and_test.sh "$@"